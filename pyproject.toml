[build-system]
requires = ["setuptools>=42", "setuptools-scm", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
# Allow building/installing without VCS metadata (e.g., when .git is absent).
fallback_version = "0.0.0"
